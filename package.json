{
  "name": "nisenotori",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "build": "vite build",
    "preview": "vite preview",
    "bdeploy": "npm run build && cp dist/index.html dist/200.html && surge dist --domain nisenotori.surge.sh"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "svelte": "^4.2.12",
    "svelte-routing": "^2.12.0",
    "vite": "^5.2.0",
    "vitest": "^1.5.2"
  },
  "dependencies": {
    "howler": "^2.2.4"
  }
}
